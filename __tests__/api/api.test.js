import 'react-test-renderer';
import weatherApi from '../../src/api/weather';

// get weather location
// error in get

describe('weatherApi', () => {
  test('getWeatherWithLocation', async () => {
    expect(2).toBe(2);
  });
});
